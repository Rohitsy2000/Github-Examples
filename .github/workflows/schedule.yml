name: 'my first workflow'
on:
    push:
        branches:
            - main
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "my first Github action job"
            - name : List files
              run: ls
            - name: Readfile
              run: cat README.md  
            
